---
name: eda-fix
description: 'Делает обычные фиксы по плану, файлу плана или короткому текстовому контексту. Перед правками читает `docs/rules.md`, `docs/arch.md` и только релевантные карточки из `docs/references/`, если они есть, и следует им с установленным приоритетом. Правит код, добавляет нужные тесты, прогоняет проверки и сохраняет историю в `docs/fixes/`. Не делает ревью и не коммитит.'
---

# Скил: Фиксер (eda-fix)

Делаешь конкретные исправления и сохраняешь отчёт, чтобы потом было понятно, что и почему правилось. Это не выполнение большого плана и не фиксы по ревью: вход может быть короткой строкой, баг-репортом, файлом плана или списком задач.

## Вход из сообщения пользователя

Текст рядом с вызовом скилла в текущем сообщении пользователя — главный вход. Сначала разбери именно его: тему, задачу, путь к файлу, режим, ограничения и прямые указания.

Если этого входа достаточно, продолжай без вопроса. `AskUserQuestion` задавай только если входа нет, он противоречивый, найдено несколько равных вариантов или есть риск выполнить не ту задачу. Старое обсуждение в истории не считай входом, если пользователь не связал его с текущим вызовом явно.

## Главные правила

1. **Перед правкой кода** прочитай `docs/rules.md` и `docs/arch.md`. Если есть `docs/references.md`, выбери по индексу только карточки для затронутых компонентов и прочитай их целиком. Приоритет: подтверждённое решение пользователя → правила → архитектура → карточки. Файлов нет — продолжай.
2. **Вход обязателен.** Если не передан план, файл или понятный текст задачи — спроси через интерактивный вопрос.
3. **Делай только указанные фиксы.** Не расширяй задачу до рефакторинга и не чини соседние проблемы без необходимости.
4. **Тесты рядом с правкой.** Меняешь поведение — добавь или обнови точечный тест.
5. **Финальная проверка обязательна.** В конце прогони подходящие тесты и линтеры проекта.
6. **Историю фиксируй в `docs/fixes/`.** Отчёт обязателен даже для маленькой правки.
7. **Не коммить и не пушь.** Это `eda-commit`. Не делай ревью — это `eda-review`.

## Интерактивные вопросы

Когда инструкция говорит `AskUserQuestion`, это означает блокирующий интерактивный вопрос.
- Claude Code: используй `AskUserQuestion`.
- Codex interactive: если доступен `request_user_input`, используй его. Если tool недоступен, задай один короткий вопрос в чат, дай варианты 1–3, напиши «Ответь номером или своим вариантом. Я продолжу только после ответа.» и остановись.
- Codex exec / неинтерактивный запуск: не задавай вопросы и не пытайся читать stdin. Если без ответа нельзя безопасно продолжать, заверши работу со статусом `blocked: нужен ответ пользователя`, перечисли вопросы и варианты, не выполняй рискованные действия.
- Не запускай команды, которые ждут интерактивного ввода в терминале; перед такими командами спроси в хост-интерфейсе или остановись с `blocked`.

## Этапы

### 1. Определить задачу
Определи `$FIX_CONTEXT`: путь к файлу прочитай, план из `docs/plans/` используй как план фикса, текст используй как контекст. Если входа нет или он неоднозначен — спроси через `AskUserQuestion`.

### 2. Прочитать рамку проекта
Прочитай `docs/rules.md` и `docs/arch.md`, если есть. Если существует `docs/references.md`, выбери по индексу только применимые карточки и используй их примеры как ожидаемую форму реализации. Если `$FIX_CONTEXT` ссылается на план с `sources.references`, прочитай перечисленные карточки; если ссылается на research, review, issue или другой файл — прочитай и его.

### 3. Сделать фикс
Найди релевантный код и внеси минимальные правки. Следуй применимому примеру; отклоняйся только по причине, подтверждённой пользователем или уже записанной во входном плане. Если задача состоит из нескольких пунктов, иди по ним последовательно. Останавливайся и спрашивай, если пункт противоречит коду или документации, требует необоснованного отклонения от карточки, продуктового решения, рискованной операции, миграции, удаления данных или затрагивает секреты.

### 4. Добавить или обновить тесты
Если фикс меняет поведение — добавь тест рядом с изменением или обнови существующий. Если тесты не нужны, в отчёте коротко объясни почему.

### 5. Финальная проверка
Прогони подходящие тесты и линтеры. Команды определи по стеку проекта. Свои поломки — исправь и повтори. Чужие или несвязанные поломки — зафиксируй в отчёте и спроси через `AskUserQuestion`, если без решения нельзя продолжать.

### 6. Сохранить отчёт
Сохрани отчёт в `docs/fixes/{YYYY-MM-DD}_{HH-MM}_{slug}.md`.

```markdown
---
date: <YYYY-MM-DD HH:MM>
source: <text | path | docs/plans/...>
status: done
---

# Фикс: <краткий заголовок>

## Контекст
<что было передано на вход и какие правила, архитектура и карточки учтены>

## Что изменено
| # | Файл | Что изменено | Зачем |
|---|------|--------------|-------|
| 1 | `path/file.ext` | ... | ... |

## Тесты и проверки
| Команда | Результат | Заметки |
|---------|-----------|---------|
| `<command>` | ✓ / ✗ | ... |

## Открытые вопросы
<если нет — «Нет»>
```

### 7. Финал
Короткое сообщение: путь к отчёту, что исправлено, какие тесты/линтеры прошли, состояние git («не закоммичено»), дальше — `eda-review` или `eda-commit`.

## Чего НЕ делать
- Коммитить, пушить, создавать PR.
- Делать ревью вместо фикса.
- Выполнять большой план с чекбоксами и журналом `docs/executions/` — это `eda-plan-execute`.
- Применять замечания из готового ревью — это `eda-fix-by-review`.
- Сохранять отчёт куда-либо кроме `docs/fixes/`.
- Игнорировать применимую карточку или молча отклоняться от её примера.
