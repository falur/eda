---
title: Незакоммиченные изменения inline-входа eda-скиллов
date: 2026-05-08 17:33
target: git diff HEAD + untracked docs/plans docs/researches docs/executions
mode: normal
score: 74
status: meta-reviewed
meta_reviewers:
  - gpt-5.4-mini
  - gpt-5.3-codex
  - gpt-5.5
---

# Ревью: Незакоммиченные изменения inline-входа eda-скиллов

## Оценка
**74/100.** Изменения закрывают основную задачу: общий раздел входа добавлен во все пакетные скиллы, `eda-plan` больше не требует выбор research по умолчанию, базовые content-тесты есть. Но в двух скиллах остались конфликтующие инструкции для новых inline-сценариев, а тесты не ловят эти конфликты.

## Замечания
| # | Тип | Где | Что не так | Что предлагаешь |
|---|-----|-----|------------|-----------------|
| 1 | bug | `docs/skills/eda-send-review.md:21` | Главное правило всё ещё говорит, что тип отправки `comment / approve / request-changes` определяется по ревью и подтверждается пользователем. Ниже новая инструкция разрешает `comment` и `review-comment` из текста без дополнительного выбора (`docs/skills/eda-send-review.md:59`). Для агента это противоречие: более раннее правило может заставить снова спрашивать подтверждение для `eda-send-review последнее ревью в PR 12 как comment`, хотя план требовал использовать `comment` из текста. Дополнительно слово `comment` смешивает два разных режима: PR comment и review без статуса. | Уточнить правило 4: подтверждение обязательно для `approve` и `request-changes`, а `comment` / `review-comment` можно брать из текущего сообщения пользователя, если нет противоречий с ревью. Явно развести `comment` как PR comment и `review-comment` как review без статуса. |
| 2 | bug | `docs/skills/eda-fix-by-review.md:47` | Скилл разрешает вставленные замечания без файла ревью и говорит сохранить отчёт с `review: text`. Но следующие этапы всё ещё ожидают файл: этап 2 требует читать «сам файл ревью» (`docs/skills/eda-fix-by-review.md:52`), этап 5 безусловно дописывает ссылку в `$REVIEW_FILE` (`docs/skills/eda-fix-by-review.md:66`), финал требует путь к ревью (`docs/skills/eda-fix-by-review.md:99`). Для inline-замечаний `$REVIEW_FILE` нет, поэтому скилл получит невыполнимую инструкцию в конце. | Развести два режима: `review_file` и `review_text`. Для `review_text` не читать `$REVIEW_FILE`, не дописывать ссылку в исходное ревью, в отчёте писать `review: text`, а в финале говорить «источник: текст из сообщения». |
| 3 | tests | `test/install.test.js:58` | Content-тесты проверяют общий раздел, `eda-plan` и `eda-research`, но не проверяют два самых рискованных специальных сценария: inline `comment` в `eda-send-review` и inline-замечания без файла в `eda-fix-by-review`. Поэтому текущие тесты зелёные, хотя оба конфликта выше уже есть в инструкциях. | После правки инструкций добавить content-тесты: `eda-send-review` не должен требовать подтверждение для безопасного `comment` / `review-comment`, но должен требовать его для `approve` / `request-changes`; `eda-fix-by-review` должен описывать отдельное поведение для `review: text` без `$REVIEW_FILE`. |

## Рекомендации
- **Править обязательно:** 1, 2
- **На усмотрение автора:** 3
- **Не править:** —

## Изменения после мета-ревью
### После Sonnet/Haiku/Opus
- **+ Добавлено:** замечание про `eda-fix-by-review`: inline-замечания без файла конфликтуют с дальнейшими шагами, которые требуют `$REVIEW_FILE`.
- **+ Добавлено:** тестовый пробел для специальных сценариев `eda-send-review comment` и `eda-fix-by-review review: text`.
- **~ Изменено:** замечание по `eda-send-review` расширено: кроме лишнего подтверждения, нужно развести PR comment и review-comment.
- **Отклонено:** новых замечаний по `eda-plan`, `eda-execute`, синхронизации `.codex/.claude` и плановым артефактам не добавлял: проверки подтвердили, что эти части соответствуют текущему плану.

## Применённые фиксы
Отчёт: `docs/review-fixes/2026-05-08_17-43_uncommitted-inline-prompt-context.md`
